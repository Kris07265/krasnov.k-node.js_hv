Homework 14 "Сервер на Express.js"

Завдання 1: Підготовка проєкту
Ініціалізуйте проєкт та встановіть Express:

bash

Copy code
npm init -y
npm install express
Очікуваний результат: у директорії з'явилися package.json, package-lock.json та папка node_modules, а в package.json у розділі dependencies є express.

Завдання 2: Міграція сервера з http на Express
У ДЗ №7 ви писали сервер на вбудованому модулі http. Тепер перепишіть його на Express.

Сервер повинен мати маршрути з ДЗ №7, але вже на Express:

GET / — головна сторінка.
GET /about — сторінка «Про нас».
GET /time — повертає поточний час (перенесіть свій код із ДЗ №7).
GET /error — статус 500 і текст Помилка сервера.
Будь-який інший маршрут — статус 404 і текст Сторінку не знайдено.
Copy code

Очікуваний результат: усі п'ять сценаріїв працюють у браузері, при цьому в коді немає жодного if (req.url === ...) і жодного writeHead.

Завдання 3: Динамічний маршрут
Додайте маршрут GET /user/:id, який повертає текст:

Copy code
Користувач з ID: <id>
Перевірте в браузері кілька варіантів: http://localhost:3000/user/1, /user/42, /user/banana.

Очікуваний результат: маршрут один, а відповідь щоразу підставляє те, що передано в URL — навіть banana.

Завдання 4: Query-параметри
Додайте маршрут GET /search, який:

читає query-параметр q і повертає Ви шукали: <q>;
якщо параметр q не передано — повертає статус 400 і текст Не вказано пошуковий запит.
Перевірте через curl у другому терміналі (не закриваючи сервер у першому):

bash

Copy code
curl -I "http://localhost:3000/search?q=Express"
Очікуваний результат: HTTP/1.1 200 OK.

bash

Copy code
curl -I http://localhost:3000/search
Очікуваний результат: HTTP/1.1 400 Bad Request.

Завдання 5: Розширення (обов'язкове)
Додайте динамічний маршрут на власний вибір, який використовує і params, і query одночасно. Наприклад, GET /user/:id/orders?status=active, що повертає:

Copy code
Замовлення користувача <id> зі статусом: <status>
Перезапустіть сервер і перевірте маршрут у браузері.